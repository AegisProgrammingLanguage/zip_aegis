[project]
name = "zip"
version = "0.1.0"
description = ""
author = "Ethan Rucar <ethan.rucar@gmail.com>"
license = "MIT"
exclude = [
    "src",
    "target",
    "Cargo.lock",
    "Cargo.toml"
]

[targets]
linux = "libzip.so"
window = "zip.dll"
macos = "libzip.dylib"

[dependencies]